title: Misusing 404.html
description: > # this means to ignore newlines until "baseurl:"
  Let's misuse the 404.html to give us support for any custom URL pattern
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
github_username:  allejo

# Build settings
markdown: kramdown
theme: minima
gems:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
